# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo

# env / secrets
.env
.env.*
!.env.example

# local database scratch files (the project uses Supabase PostgreSQL; these
# patterns just guard against stray local DB files being committed)
data/
*.db
*.db-journal
*.sqlite

# logs / os
*.log
.DS_Store
Thumbs.db

reports
